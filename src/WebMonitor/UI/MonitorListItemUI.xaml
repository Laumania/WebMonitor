<UserControl x:Class="WebMonitor.UI.MonitorListItemUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Width="150" Height="100" Background="{x:Null}">
    <Border CornerRadius="5">
    	<Border.Background>
    		<LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
    			<GradientStop Color="#FFCBCED2" Offset="0"/>
    			<GradientStop Color="#FF54A4FF" Offset="1"/>
    		</LinearGradientBrush>
    	</Border.Background>
        <Grid>
        	<Grid.RowDefinitions>
        		<RowDefinition Height="0.223*"/>
        		<RowDefinition Height="0.777*"/>
        	</Grid.RowDefinitions>
            <TextBlock Text="{Binding Path=Url}" Grid.RowSpan="2" FontSize="16" FontWeight="Bold" />
        </Grid>
    </Border>
</UserControl>
